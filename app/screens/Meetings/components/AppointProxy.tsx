import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AppointProxy = () => {
  return (
    <View>
      <Text>AppointProxy</Text>
    </View>
  );
};

export default AppointProxy;

const styles = StyleSheet.create({});
